{
  "name": "security-report-automation-web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:web": "python3 scripts/build_web_report.py && python3 scripts/validate_web_report.py",
    "deploy": "wrangler deploy",
    "dev": "python3 scripts/build_web_report.py && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.116.0"
  }
}
